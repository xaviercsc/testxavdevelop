pipeline {
  agent any
  stages {
    stages("build") {
      steps {
        echo 'Building application..'
      } 
      }
  stages("Deploy") {
      steps {
        echo 'deploying application..'
      } 
      }  
  }
  }
